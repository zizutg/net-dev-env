worker_processes auto;

events {
    worker_connections 1024;
}

stream {
    upstream echoservers {
        server echoserver:5555;
        server echoserver_1:5555;
        server echoserver_2:5555;
    }

    server {
        listen 9000;
        proxy_pass echoservers;
    }
}